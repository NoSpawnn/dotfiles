# Sane input defaults

input type:pointer {
	accel_profile "flat"
}

input type:touchpad {
    accel_profile "flat"
    dwt enabled
    middle_emulation enabled
    tap enabled
}

input type:keyboard {
    repeat_delay 200
    repeat_rate 35
}
