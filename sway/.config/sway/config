### Variables ###

set $SWAY_CONF_DIR $HOME/.config/sway

# Logo key
set $mod Mod4

set $left h
set $down j
set $up k
set $right l

# Default programs

set $term ghostty
set $menu wofi --show drun -c ~/.config/wofi/config -G

# Include the other stuff
include ./shared/*

# Thanks https://jumptuck.com/blog/2025-05-26-hyprland-conditional-config/ for the inspiration :3
include ./device-custom/$HOSTNAME

include /etc/sway/config.d/*
